{% extends '@adminkit/base.html.twig' %}

{% block title %}Eventos | {{backend_name}} {% endblock %}

{% block body %}
    <div class="d-flex justify-content-between mb-3">
        <h1 class="h3 d-inline align-middle">Eventos</h1>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header pb-0">
                    
                    <h5 class="card-title mb-0">Editar Evento</h5>
                </div>
                <div class="card-body">
                        {{ include('@backend/event/_form.html.twig', {'button_label': 'Atualizar'}) }}

                    <div class="d-flex bd-highlight my-3">
                        <a class="me-auto btn btn-link px-0" href="{{ path('app_event_index') }}">Voltar para lista</a>
                        {{ include('@backend/event/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
